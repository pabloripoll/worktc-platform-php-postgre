FROM mailhog/mailhog:latest

# Expose SMTP (1025) and HTTP UI (8025)
EXPOSE 1025 8025

# MailHog's image already provides the correct entrypoint/cmd
# leaving default behavior so container starts MailHog.